# Dummy values for rendering the compose model in tests. Nothing here is a real
# credential, and nothing the harness renders with it contains one.
POSTGRES_USER=homecrew
POSTGRES_PASSWORD=homecrew
POSTGRES_DB=homecrew
CONFIG_GIT_USERNAME=hc-test-user
CONFIG_GIT_TOKEN=hc-test-token
ENCRYPT_KEY=hc-test-encrypt-key-not-a-secret
CONFIG_CLIENT_PASSWORD=localdev
