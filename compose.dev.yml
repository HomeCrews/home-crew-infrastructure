# Dev override: every service runs from your checkout, hot reloading.
#
# Used together with docker-compose.yml, which ./dev does for you:
#
#     docker compose -f docker-compose.yml -f compose.dev.yml up -d
#
# LOCAL IMAGES ONLY. Every service here is rebuilt from Dockerfile.dev in this
# repository and tagged homecrew-dev-runtime:jdk25, which replaces the
# mthanuj/homecrew-*:dev image from the base file. Nothing is ever pulled from
# Docker Hub, so a stale published tag cannot quietly replace the code you are
# editing. One image serves all twelve: it contains a JDK and nothing else,
# because the application arrives on a bind mount.
#
# Never used by the deploy. .github/workflows/deploy.yml runs plain
# `docker compose` with no -f flags and only scps docker-compose.yml to the
# host, so this file cannot reach production even by accident.
#
# Each service gets a debugger on its own port, 5005 upwards in the order
# below. DevTools restarts happen inside the same JVM, so an attached debugger
# survives them.
#
# OWN target/ PER CONTAINER. Every service mounts a named volume over
# /app/target, so the container compiles into a directory the host never sees.
# Sharing the checkout's target/ meant two compilers writing one tree: the VS
# Code Java extension builds into target/classes on the host, and DevTools in
# the container restarts on any write it sees there. One save became two
# restarts, racing each other over the same class files, and anything that makes
# the IDE rebuild the workspace - opening it, a pull, a pom change - restarted
# every service at once, config-server included, so the rest failed their config
# fetch. A host `./mvnw clean verify`, which the pre-push hook runs, also deleted
# the classes out from under the running application.
#
# The cost: an IDE compile-on-save no longer reaches the container, so
# dev-reload.sh's own compile is the only way code gets in. The volumes are
# caches - `docker compose down -v` empties them along with the databases, and
# the next start compiles from cold.

services:
  service-discovery:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-service-discovery:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - service_discovery_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5005:5005"
    healthcheck:
      start_period: 240s
      retries: 40

  config-server:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-config-server:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - config_server_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5006"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5006:5006"
    healthcheck:
      start_period: 240s
      retries: 40

  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-api-gateway:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - api_gateway_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5007"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5007:5007"

  auth-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-auth-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - auth_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5008"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5008:5008"

  user-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-user-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - user_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5009"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5009:5009"

  admin-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-admin-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - admin_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5010"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5010:5010"

  booking-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-booking-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - booking_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5011"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5011:5011"

  worker-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-worker-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - worker_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5012"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5012:5012"

  notification-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-notification-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - notification_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5013"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5013:5013"

  payment-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-payment-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - payment_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5014"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5014:5014"

  xp-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-xp-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - xp_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5015"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5015:5015"

  assignment-service:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: homecrew-dev-runtime:jdk25
    pull_policy: never
    entrypoint: ["/bin/sh", "/dev-reload.sh"]
    working_dir: /app
    mem_limit: ${DEV_SERVICE_MEM:-1g}
    volumes:
      - ../home-crew-assignment-service:/app
      # Container-local target/ - see OWN target/ PER CONTAINER above.
      - assignment_service_target:/app/target
      - ${HOME}/.m2:/root/.m2
      - ./dev-reload.sh:/dev-reload.sh:ro
    environment:
      # Emptied, not tuned. The production value is -Xmx128m and
      # JAVA_TOOL_OPTIONS applies to EVERY JVM in the container, so it would
      # cap Maven at 128m too and make the compile loop crawl.
      JAVA_TOOL_OPTIONS: ""
      MAVEN_OPTS: "-Xmx256m"
      # Only the forked application JVM gets the debug agent. In
      # JAVA_TOOL_OPTIONS, Maven and every compile-loop JVM would try to bind
      # this port as well, every two seconds.
      DEV_JVM_ARGS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5016"
      SPRING_DEVTOOLS_RESTART_POLL_INTERVAL: 2s
      SPRING_DEVTOOLS_RESTART_QUIET_PERIOD: 1s
      # Passed through explicitly. .env drives compose SUBSTITUTION, not
      # container environment, so dev-reload.sh would never see it otherwise -
      # it would silently fall back to its own default and the knob in .env
      # would look broken rather than absent.
      DEV_RELOAD_INTERVAL: ${DEV_RELOAD_INTERVAL:-2}
    ports:
      - "5016:5016"

  # The one service that is not a JVM. Angular's dev server does its own
  # watching and rebuilding, so there is no compile loop to bolt on - see
  # webapp-dev.sh. It gets node:24-alpine rather than Dockerfile.dev, which
  # carries a JDK and Maven it has no use for.
  webapp:
    image: node:24-alpine
    entrypoint: ["/bin/sh", "/webapp-dev.sh"]
    working_dir: /app
    mem_limit: ${WEBAPP_DEV_MEM:-1g}
    volumes:
      - ../home-crew-webapp:/app
      # node_modules in a named volume, NOT on the bind mount. Installing onto
      # the host directory would be slow over a macOS bind mount, would leave a
      # platform-specific tree in your checkout, and would collide with any
      # install you run outside the container.
      - webapp_node_modules:/app/node_modules
      - ./webapp-dev.sh:/webapp-dev.sh:ro
    environment:
      WEBAPP_POLL_INTERVAL: ${WEBAPP_POLL_INTERVAL:-2000}

volumes:
  webapp_node_modules:
  service_discovery_target:
  config_server_target:
  api_gateway_target:
  auth_service_target:
  user_service_target:
  admin_service_target:
  booking_service_target:
  worker_service_target:
  notification_service_target:
  payment_service_target:
  xp_service_target:
  assignment_service_target:
